{
  "commands": [
    "pkill rdi-proxy || true",
    "sleep 2",
    "cd /opt/rdi-proxy && nohup ./rdi-proxy >> /var/log/rdi-proxy.log 2>&1 &",
    "sleep 5",
    "for attempt in 1 2 3 4 5; do curl -s -o /dev/null -w 'HTTP 8766: %{http_code}' --connect-timeout 3 http://127.0.0.1:8766/ && break; sleep 2; done || exit 1",
    "ss -tlnp | grep -E '8765|8766' || true"
  ]
}
